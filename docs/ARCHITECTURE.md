# Architecture

Current demo:

Angular -> REST -> Spring Boot -> Spring Data JPA -> MySQL

The application is intentionally monolithic for the demo. Later it can be split into auth, student, academic, faculty, attendance, examination, task, library, finance, document, placement, hostel, transport, notification and reporting services behind an API Gateway.
